<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- REMOVE this line: android.permission.USB_PERMISSION does not exist and is ignored by Android -->
    <!-- <uses-permission android:name="android.permission.USB_PERMISSION" /> -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Keep this for handling USB attach/detach events if needed, but we'll use a BroadcastReceiver for permission results -->
        <intent-filter>
            <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
        </intent-filter>
        <!-- Optional: Meta-data for auto-detecting your USB device (replace with your device's vendor/product ID) -->
        <meta-data android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                   android:resource="@xml/device_filter" />
      </activity>

      <!-- Add a BroadcastReceiver to handle the permission result intent -->
      <receiver android:name=".UsbPermissionReceiver" android:exported="false">
        <intent-filter>
          <action android:name="com.smartcardpoc.USB_PERMISSION" />
        </intent-filter>
      </receiver>
    </application>
</manifest>